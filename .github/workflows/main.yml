name: Build and Deploy Hugo Site

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: write  # 需要写入权限来推送gh-pages分支
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
        with:
          submodules: true
          fetch-depth: 0

      - name: Setup Hugo
        uses: peaceiris/actions-hugo@v2
        with:
          hugo-version: '0.115.2'
          extended: true

      - name: Build with Hugo
        run: hugo --minify --baseURL https://xinhuizhineng.github.io/blog/  

      # 4️⃣ 检测图片是否生成
      - name: Check generated images
        run: |
          echo "📂 检查 public/images 目录内容..."
          if [ -d "public/images" ]; then
            ls -R public/images
            if [ -z "$(ls -A public/images)" ]; then
              echo "❌ 构建完成但 public/images 目录为空，请检查 static/images 是否有文件"
              exit 1
            fi
          else
            echo "❌ 构建完成但 public/images 目录不存在，请检查图片路径"
            exit 1
          fi

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./public
          publish_branch: gh-pages
          commit_message: ${{ github.event.head_commit.message }}
